services:
  - type: web
    name: vibesync-backend
    env: node
    buildCommand: npm install
    healthCheckPath: /
    startCommand: node server.js
    envVars:
      - key: FRONTEND_URL
        value: "*"
